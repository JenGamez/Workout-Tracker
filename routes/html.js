const router = require("express").Router();
const Workout = require("../models/workout.js")
// REQUIRE PATH

router.get("/exercise", (req, res) => {
  res.send("..model/index.html")
// path __dirname yada yada
  });

  

  module.exports = router;
